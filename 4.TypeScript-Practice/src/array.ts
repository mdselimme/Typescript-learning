



//Array in ts

// array declare method 
const name: string[] = ['sleim', 'samun', 'yousuf'];
name.push('habub');
// name.push(1)

// array readonly method cannot added any property

const names: readonly string[] = ['savar', 'user', 'kahta'];
// names.push("hdd") //cannot push because its readonly method

// type interface method 

const number = [1, 2, 3, 411, 6];
number.push(56)
// number.push("ds")

let first: number = number[0];






export { }